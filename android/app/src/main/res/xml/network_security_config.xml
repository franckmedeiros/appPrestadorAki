<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfego HTTP não criptografado (cleartext) apenas para os
  endereços usados pelos emuladores locais do Firebase (`firebase
  emulators:start`) durante o desenvolvimento — necessário porque o
  Android bloqueia cleartext por padrão a partir da API 28, e o SDK do
  Firebase Auth/Firestore fala HTTP puro (não HTTPS) com o emulador. Não
  afeta a comunicação com o Firebase de produção, que sempre usa HTTPS e
  não passa por essa configuração. 10.0.2.2 é o endereço especial que o
  emulador Android usa pra alcançar o "localhost" da máquina host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
